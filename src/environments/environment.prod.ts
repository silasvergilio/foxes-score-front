/**
 * Production environment. Swapped in for environment.ts by the
 * `fileReplacements` entry in angular.json's production config.
 *
 * Keep the export shape identical to environment.ts.
 */
export const environment = {
  production: true,
  apiUrl: 'https://foxes-score-backend-19a6f4f9b2c5.herokuapp.com',
};
